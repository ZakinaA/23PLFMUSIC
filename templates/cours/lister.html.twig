<!DOCTYPE html>
<html>
<head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

<h2>LISTE DES COURS</h2>
<table border="1">
        <tr>
                <th>Heure de début</th>
                <th>Heure de fin</th>
                <th>Nombre de places</th>
                <th>Age minimum</th>
                <th>Age maximum</th>
                <th>Type de cours</th>
                <th>Jour</th>
                <th>Type d'instrument</th>
        </tr>
        {% for c in pLesCours %}
                <tr>

                        <td>{{ c.heureDebut | format_time }}</td>
                        <td>{{ c.heureFin | format_time }}</td>
                        <td>{{ c.nbPlaces }}</td>
                        <td>{{ c.ageMini }}</td>
                        <td>{{ c.ageMaxi }}</td>
                        <td>{{ c.typeCours.libelle }}</td>
                        <td>{{ c.jour.libelle }}</td>
                        <td>{{ c.typeInstrument.libelle }}</td>
                </tr>
        {% else %}
                <tr>
                        <td colspan="8">Aucun cours n'a été trouvé.</td>
                </tr>
        {% endfor %}
</table>
</body>
</html>
