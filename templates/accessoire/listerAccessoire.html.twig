<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Liste des accessoires{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

LISTE DES ACCESSOIRES</br>
<table>


    <th>Libelle </th>
    <th> Instrument </th>





    {% for accessoires in accessoires %}
        <tr>


        <td>{{ accessoires.libelle }}</a></td>
            <td><a href="{{ path('InstrumentConsulter', { 'id': accessoires.instrument.id }) }}">{{ accessoires.instrument.nom }}</a></td>
<td>
            <form method="post" action="{{ path('accessoireConsulter', {'id': accessoires.id}) }}">
                <button type="submit">Consulter</button>
            </form></td>

<td>
        <form method="post" action="{{ path('accessoireModifier', {'id': accessoires.id}) }}">
            <button type="submit">Modifier</button>
        </form></td>

<td>
       <form method="post" action="{{ path('accessoireSupprimer', {'id': accessoires.id}) }}">
           <button type="submit">Supprimer</button>
       </form></td>



    {% else %}
        <tr>
            <td>Aucun instrument n'a été trouvé.</td>
        </tr>
    {% endfor %}

</table>


</body>
</html>